DATABASE_URL=postgres://user:password@localhost:5432/roastline

# Used only by `pnpm run seed` to create the first login (dev convenience, optional)
SEED_OWNER_EMAIL=owner@roastline.test
SEED_OWNER_PASSWORD=roastline-dev
